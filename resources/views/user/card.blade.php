<a class="extra-card-link" <!--data-reveal-id="extraModal" data-reveal-ajax="{{ route('extra', $id) }}"-->><div class="extra-card">
   <img src="{{ $image }}" alt="" />
   <span class="title">{{ $title }}</span>
   <p class="description">{{ $description }}</p>
</div></a>
